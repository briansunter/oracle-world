# Shared variable defaults
#
# These values are consistent across environments.
# This file is NOT imported directly - it serves as documentation
# and a single source of truth for shared defaults.
#
# Currently empty — all variables are defined per-module.
